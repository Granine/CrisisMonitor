apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: mlapp
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 80        # service port (what port-forward svc:local maps to)
      targetPort: 80  # container port inside pod
      protocol: TCP
